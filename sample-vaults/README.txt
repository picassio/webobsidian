Optional Docker bind-mount parent for additional vault directories.

Set VAULTS_HOST_PATH in .env to your real common parent, then register individual
/vaults/<name> directories in Settings. Do not register this placeholder folder.
